# matlab-add-on-library

Add some little tools to write Matlab code more fluently, especially in the live script.

## Way to use

- Add the path to Matlab.
- Use `help myLib` to show all the available functions.